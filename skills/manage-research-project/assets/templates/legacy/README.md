# {{PROJECT_NAME}}

Project type: {{PROJECT_TYPE}}
Current stage: {{STAGE}}

{{OBJECTIVE}}

Start with `docs/PROJECT_STATUS.md` and `docs/SESSION_HANDOFF.md`.
Read `docs/PROJECT_BRIEF.md`, `docs/DATA_DICTIONARY.md` and
`docs/ANALYSIS_PLAN.md` when their context is needed.

This project preserves its existing layout. Use `PROJECT_MAP.json` when present
to resolve document roles, source roots, raw data, native runs and deliverables.
Without a map, inspect and record the existing entry points before writing.
Creating project records does not declare layout v2 or authorize moving files.

Source roots: {{SOURCE_ROOTS}}
Protected raw roots: {{RAW_ROOTS}}
Native runs: {{RUN_ROOT}}
Reviewed results: {{RESULTS_ROOT}}

Provenance registries and formal specifications, when used, remain under
`provenance/`, `analysis/specs/` and `analysis/readiness/`.
Treat each actual run as preserved evidence. Record delivered copies and their
source; check copied snapshots before declaring archive completion.
